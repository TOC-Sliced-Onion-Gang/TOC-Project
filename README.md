# TOC-Project
## To run
### Download the dependencies
`pip install -r requirement.txt`

### Export run shortcut

for mac, linux
`export FLASK_APP=run.py`

for windows
`set FLASK_APP=run.py`

### Run app
`flask run`

